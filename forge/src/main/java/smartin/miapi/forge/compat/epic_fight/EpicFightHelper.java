package smartin.miapi.forge.compat.epic_fight;

public class EpicFightHelper {

    /*
    @Nullable
    public static CapabilityItem fromJsonElement(Item item, JsonElement element) throws CommandSyntaxException {
        return ItemCapabilityReloadListener.deserializeWeapon(item, NbtHelper.fromNbtProviderString(element.getAsString()), null);
    }

    @Nullable
    public static CapabilityItem fromJsonElement(ItemStack itemStack){
        try{
            JsonElement element = EpicFightCompatProperty.property.getJsonElement(itemStack);
            if(element!=null){
                return fromJsonElement(itemStack.getItem(),element);
            }
        } catch (Exception ignored) {
        }
        return null;
    }
     */
}
