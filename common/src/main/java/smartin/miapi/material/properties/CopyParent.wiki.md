@header Allowed Material
@path /datapack/material/properties/copy_parent

This Property manages the allowed materials for a module.

Example
```json5
{
    "copy_parent_material": "true",
    // copies the Parents material if this doesn't have a material set
}
```
